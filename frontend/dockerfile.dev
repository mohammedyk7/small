FROM node:20-alpine

WORKDIR /app

# Copy package.json only first (cache layer)
COPY package*.json ./

RUN npm install

# Copy the rest of the app
COPY . .

EXPOSE 5173

# Run Vite dev server with host enabled for Docker
CMD ["npm", "run", "dev", "--", "--host"]
